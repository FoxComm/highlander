{
    "variables" : {
        "base_node_image": "base-node-1466534128",
        "project_id": "foxcomm-staging",
        "network": "default"        
    },
    "builders" : [
        {
            "type": "googlecompute",
            "account_file": "account.json",
            "project_id" : "{{user `project_id`}}",
            "source_image": "{{user `base_node_image`}}",
            "network": "{{user `network`}}",
            "zone": "us-central1-f",
            "image_name": "vanilla-front-{{timestamp}}",
            "image_description": "vanilla front image",
            "instance_name": "packer-vanilla-front-{{uuid}}",
            "machine_type": "n1-standard-1",
            "tags": ["ssh", "no-ip", "vanilla", "front"],
            "use_internal_ip": true,
            "ssh_username" : "{{user `ssh_username`}}"
        }
    ],
    "provisioners" : [
        {
            "type": "ansible",
            "playbook_file" : "ansible/packer_vanilla_front.yml"
        }
    ]
}
