{
    "variables" : {
        "base_jvm_image": "base-jvm-1466533418",
        "project_id": "foxcomm-staging",
        "network": "default"        
    },
    "builders" : [
        {
            "type": "googlecompute",
            "account_file": "account.json",
            "project_id" : "{{user `project_id`}}",
            "source_image": "{{user `base_jvm_image`}}",
            "network": "{{user `network`}}",
            "zone": "us-central1-f",
            "image_name": "vanilla-green-{{timestamp}}",
            "image_description": "vanilla greenriver image",
            "instance_name": "packer-vanilla-green-{{uuid}}",
            "machine_type": "n1-standard-1",
            "tags": ["ssh", "no-ip", "vanilla", "greenriver"],
            "use_internal_ip": true,
            "ssh_username" : "{{user `ssh_username`}}"
        }
    ],
    "provisioners" : [
        {
            "type": "ansible",
            "playbook_file" : "ansible/packer_vanilla_greenriver.yml"
        }
    ]
}
