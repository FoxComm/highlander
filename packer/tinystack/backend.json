{
    "builders" : [
        {
            "type": "googlecompute",
            "account_file": "{{user `account_file`}}",
            "project_id" : "{{user `project_id`}}",
            "source_image": "{{user `base_jvm_image`}}",
            "network": "{{user `network`}}",
            "zone": "{{user `zone`}}",
            "image_name": "{{user `image_prefix`}}-backend-{{timestamp}}",
            "image_description": "{{user `image_title`}} Backend Image",
            "instance_name": "packer-{{user `image_prefix`}}-backend-{{uuid}}",
            "machine_type": "n1-standard-2",
            "tags": ["ssh", "no-ip", "{{user `image_prefix`}}", "backend"],
            "use_internal_ip": true,
            "ssh_username" : "{{user `ssh_username`}}"
        }
    ],
    "provisioners" : [
        {
            "type": "ansible",
            "playbook_file" : "ansible/packer_tinystack_backend.yml",
            "extra_arguments": ["--extra-vars", "@packer/envs/staging_ansible.json"]
        }
    ]
}