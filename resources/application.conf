db {
  connectionPool = HikariCP
  driverClassName = "org.postgresql.Driver"

  url    = "jdbc:postgresql://localhost/phoenix_development?user=phoenix"
  driver = "slick.driver.PostgresDriver"
  user   = "phoenix"

  # Repetion currently necessary to get the right source for Flyway
  host   = "127.0.0.1"
  name   = "phoenix_development"
}

# TODO: setup for environments
akka {
  loggers        = ["akka.event.slf4j.Slf4jLogger"]
  loglevel       = "DEBUG"
  logging-filter = "akka.event.slf4j.Slf4jLoggingFilter"
}

http {
  interface = "localhost"
  port = 9090
}

env {
  development {
    akka.loglevel = "DEBUG"
    akka.log-dead-letters-during-shutdown = on
  }

  staging = ${env.development} {
    http.interface = "0.0.0.0"
  }

  test = ${env.development} {
    db.url    = "jdbc:postgresql://localhost/phoenix_test?user=phoenix"
    db.name   = "phoenix_test"
  }
}
