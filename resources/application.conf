db {
  development {
    connectionPool = HikariCP
    driverClassName = "org.postgresql.Driver"

    url    = "jdbc:postgresql://localhost/phoenix_development?user=phoenix"
    driver = "slick.driver.PostgresDriver"
    user   = "phoenix"

    # Repetion currently necessary to get the right source for Flyway
    host   = "127.0.0.1"
    name   = "phoenix_development"
  }

  test = ${db.development} {
    url    = "jdbc:postgresql://localhost/phoenix_test?user=phoenix"
    name   = "phoenix_test"
  }
}
