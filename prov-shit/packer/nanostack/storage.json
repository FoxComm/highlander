{
    "builders" : [
        {
            "name": "google",
            "type": "googlecompute",
            "account_file": "{{user `account_file`}}",
            "project_id" : "{{user `project_id`}}",
            "source_image": "{{user `base_nano_image`}}",
            "network": "{{user `network`}}",
            "zone": "us-central1-a",
            "image_name": "nanostack-storage-{{timestamp}}",
            "image_description": "Base Nanostack Storage Image",
            "instance_name": "packer-nanostack-storage-{{uuid}}",
            "machine_type": "n1-standard-2",
            "tags": ["ssh", "no-ip"],
            "use_internal_ip": true,
            "ssh_username" : "{{user `ssh_username`}}",
            "disk_size": 20
        }
    ],
    "provisioners" : [
        {
            "type": "ansible",
            "playbook_file" : "ansible/packer_nanostack_storage.yml",
            "extra_arguments": ["-vvvv"]
        }
    ]
}
