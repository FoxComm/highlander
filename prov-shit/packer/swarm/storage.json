{
    "builders": [
        {
            "name": "google",
            "type": "googlecompute",
            "account_file": "{{user `account_file`}}",
            "project_id": "{{user `project_id`}}",
            "source_image": "{{user `base_image`}}",
            "network": "{{user `network`}}",
            "zone": "{{user `zone`}}",
            "image_name": "swarm-storage-{{isotime \"060102-150405\"}}",
            "image_description": "Storage image used for provisioning",
            "instance_name": "packer-swarm-storage-{{uuid}}",
            "machine_type": "{{user `instance_type`}}",
            "tags": [
                "no-ip",
                "packer-swarm-storage"
            ],
            "use_internal_ip": true,
            "ssh_username": "{{user `ssh_username`}}"
        }
    ],
    "provisioners": [
        {
            "type": "ansible",
            "playbook_file": "ansible/pack_swarm_storage.yml",
            "extra_arguments": [
                "-vvvv"
            ]
        }
    ]
}
