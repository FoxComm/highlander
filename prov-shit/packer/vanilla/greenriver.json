{
    "builders" : [
        {
            "name": "google",
            "type": "googlecompute",
            "account_file": "{{user `account_file`}}",
            "project_id" : "{{user `project_id`}}",
            "source_image": "{{user `base_jvm_image`}}",
            "network": "{{user `network`}}",
            "zone": "{{user `zone`}}",
            "image_name": "{{user `image_prefix`}}-green-{{isotime \"060102-150405\"}}",
            "image_description": "{{user `image_title`}} Green River Image",
            "instance_name": "packer-{{user `image_prefix`}}-green-{{uuid}}",
            "machine_type": "n1-standard-1",
            "tags": ["ssh", "no-ip", "{{user `image_prefix`}}", "greenriver"],
            "use_internal_ip": true,
            "ssh_username" : "{{user `ssh_username`}}"
        },
        {
            "name": "amazon",
            "type": "amazon-ebs",
            "source_ami": "{{user `base_jvm_ami`}}",
            "instance_type": "{{user `instance_type`}}",
            "region": "{{user `region`}}",
            "ami_name": "{{user `image_prefix`}}-green-{{isotime \"060102-150405\"}}",
            "ami_description": "{{user `image_title`}} Green River Image",
            "associate_public_ip_address": true,
            "ssh_username" : "{{user `ssh_username`}}",
            "vpc_id": "{{user `vpc_id`}}",
            "security_group_id": "{{user `security_group_id`}}",
            "subnet_id": "{{user `subnet_id`}}",
            "tags": {
                "Name": "{{user `image_prefix`}}-greenriver-{{isotime \"060102-150405\"}}"
            }
        }
    ],
    "provisioners" : [
        {
            "type": "ansible",
            "playbook_file" : "ansible/packer_service_greenriver.yml",
            "extra_arguments": [
                "-vvvv",
                "--extra-vars", "@packer/envs/{{user `project_base`}}/params.json"
            ]
        }
    ]
}
