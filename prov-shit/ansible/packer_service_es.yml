---

- name: Provision Packer Vanilla ES
  hosts: all
  tags:
      - packer-vanilla-es
  vars:
    user: ubuntu
    packing: true
    es_heap_size: 4000m
    search_service_name: elasticsearch
    log_host: es-log.service.consul
  post_tasks:
    - name: Sweet Dreams
      raw: sleep 5

  roles:
    - { role: base/rsyslog, sudo: yes }
    - { role: base/consul_agent }
    - { role: base/elasticsearch }
    - { role: base/kibana, sudo: yes }
