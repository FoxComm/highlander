---

- name: Update phoenix
  hosts: "{{hostname}}"
  vars:
    user: '{{ansible_ssh_user}}'
    upload_phoenix_jar: true
  vars_prompt:
    - name: "hostname"
      prompt: "Enter a hostname with a phoenix.service"
      private: no

  roles:
    - { role: dev/phoenix }
