---

- name: Packing Base Mesos Image
  hosts: all
  vars:
    user: ubuntu
    packing: true
  post_tasks:
    - name: Sweet Dreams
      pause: seconds=5

  roles:
    - { role: base/docker }
    - { role: base/mesos }
    - { role: base/consul_agent }
