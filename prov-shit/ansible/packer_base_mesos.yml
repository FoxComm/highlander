---

- name: Provision All The Things
  hosts: all
  vars:
    user: ubuntu
    packing: true
  roles:
    - { role: base/docker }
    - { role: base/mesos }
  post_tasks:
    - name: Sweet Dreams
      pause: seconds=5    

  handlers:
     - include: roles/base/consul_agent/handlers/main.yml
