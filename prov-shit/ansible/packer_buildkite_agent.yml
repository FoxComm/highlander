---

- name: Pack Buildkite Agent image
  hosts: all
  tags:
    - packer
  vars:
    user: '{{ansible_ssh_user}}'

  roles:
    - { role: base/common }
    - { role: base/ssh_keys }
    - { role: base/scala }
    - { role: base/node }
    - { role: test/db }
    - { role: test/selenium }
    - { role: base/builder }
    - { role: dev/ssh_gce }
