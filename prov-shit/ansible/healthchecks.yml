---

- name: Perform Consul and Marathon Healthchecks
  hosts: '{{hostname}}'
  vars_prompt:
    - name: "hostname"
      prompt: "Enter a hostname"
      private: no
  roles:
    - { role: dev/healthcheck }
