---

- name: Packing Base Swarm Image
  hosts: all
  tags:
    - packer
  vars:
    user: ubuntu
  gather_facts: no

  pre_tasks:
    - name: Install Python 2 and Boto
      apt: name={{item}} state=latest force=yes
      sudo: true
      with_items:
        - python2.7
        - python-boto
    - name: Gather Facts
      setup:

  sudo: true
  roles:
    - { role: swarm/base }
    - { role: swarm/mesos/base }
    - { role: swarm/docker }
