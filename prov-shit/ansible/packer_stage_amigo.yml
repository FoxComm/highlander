---

- name: Packing Stage Amigo
  hosts: all
  vars:
    user: ubuntu
    packing: true
    join_type: join
    mesos_quorum: 1
    zookeeper_server: "localhost:2181"

  roles:
    - { role: base/consul_server }
    - { role: base/zookeeper }
    - { role: base/docker_registry }
    - { role: base/mesos_master }
    - { role: base/mesos_consul }
