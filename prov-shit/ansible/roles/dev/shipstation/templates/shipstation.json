{
  "id": "/shipstation",
  "cmd": null,
  "cpus": 0.25,
  "mem": 64,
  "disk": 0,
  "instances": 0,
  "container": {
    "type": "DOCKER",
    "volumes": [],
    "docker": {
      "image": "{{docker_registry}}:5000/shipstation-consumer:{{docker_branch}}",
      "network": "HOST",
      "privileged": false,
      "parameters": [],
      "forcePullImage": true
    }
  },
  "env": {
    "SCHEMA_REGISTRY_URL": "http://{{schema_server}}",
    "ZOOKEEPER_URL": "{{zookeeper_server}}",
    "MIDDLEWAREHOUSE_URL": "http://{{middlewarehouse_server}}",
    "API_KEY": "{{shipstation_api_key}}",
    "API_SECRET": "{{shipstation_api_secret}}",
    "TOPIC": "{{shipstation_topic}}",
    "PARTITION": "{{shipstation_partition}}",
    "POLLING_INTERVAL": "{{shipstation_interval}}"
  },
  "portDefinitions": [
    {
      "port": 10000,
      "protocol": "tcp",
      "labels": {}
    }
  ],
  "healthChecks": [
    {
      "protocol": "COMMAND",
      "command": {
        "value": "ps -ef | grep -v grep | grep shipstation-consumer"
      },
      "gracePeriodSeconds": 300,
      "intervalSeconds": 60,
      "timeoutSeconds": 20,
      "maxConsecutiveFailures": 3
    }
  ]
}
