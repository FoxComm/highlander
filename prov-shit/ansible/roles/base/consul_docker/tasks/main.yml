---

- name: Copy Service
  template: src=registrator.service dest="{{dest_services}}/registrator.service"
  sudo: true

- name: Reload Systemd for Registrator Server
  sudo: yes
  command: systemctl daemon-reload

- name: Restart Registrator Server
  service: name=registrator state=restarted enabled=yes
  sudo: true
