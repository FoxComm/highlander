---

  - name: create client config
    template: src=client.json dest={{ sensu_config_path }}
    notify: restart sensu-client
    sudo: true

  - name: start client
    service: name=sensu-client state=started enabled=yes
    sudo: true
