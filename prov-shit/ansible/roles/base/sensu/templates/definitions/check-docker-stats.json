{
  "checks": {
    "docker-stats": {
      "command": "metrics-docker-stats.rb -p unix -H /var/run/docker.sock",
      "interval": 60,
      "subscribers": [
        "consul-server",
        "frontend",
        "backend"
      ]
	}
  }
}
