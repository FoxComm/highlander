{
  "checks": {
    "netstat-sock": {
      "command": "metrics-sockstat.rb",
      "interval": 60,
      "subscribers": [
        "consul-server",
        "frontend",
        "backend"
      ]
    }
  }
}
