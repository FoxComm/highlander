{
  "checks": {
    "cpu-load": {
      "command": "check-load.rb -w 5,6,7 -c 9.9,9.5,9",
      "interval": 60,
      "subscribers": {{ group_names | to_nice_json }}
	}
  }
}





 "ansible_default_ipv4": {
            "address": "10.240.0.54"
			
			
"ansible_hostname": "foxcomm-stage-sensu-master"



{{ hostvars['consul-server']['ansible_default_ipv4'] }}

{% if 'webserver' in group_names %}
   # some part of a configuration file that only applies to webservers
{% endif %}

"subscriptions": {{ group_names | to_nice_json }}