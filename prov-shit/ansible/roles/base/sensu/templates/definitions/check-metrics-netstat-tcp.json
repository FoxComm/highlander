{
  "checks": {
    "netstat-tcp": {
      "command": "metrics-netstat-tcp.rb",
      "interval": 60,
      "subscribers": [
        "consul-server",
        "frontend",
        "backend"
      ]
    }
  }
}
