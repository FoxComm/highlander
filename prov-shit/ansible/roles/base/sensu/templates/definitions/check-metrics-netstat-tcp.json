{
  "checks": {
    "netstat-tcp": {
      "command": "metrics-netstat-tcp.rb",
      "interval": 60,
      "subscribers": [
        "all",
        "consul-server",
        "frontend",
        "backend"
      ]
    }
  }
}
