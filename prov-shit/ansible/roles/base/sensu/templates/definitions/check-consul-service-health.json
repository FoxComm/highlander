{
  "checks": {
    "consul-service-health": {
      "command": "check-consul-service-health.rb -a",
      "interval": 60,
      "subscribers": [
        {{ ansible_hostname | to_nice_json }}
      ]
    }
  }
}
