{
  "client": {
    "name": "{{ ansible_hostname }}",
    "address": "{{ ansible_default_ipv4['address'] }}",
	"datacenter": "test",
      "subscriptions": [
	    "{{ ansible_hostname }}"
      ]
  }
}
