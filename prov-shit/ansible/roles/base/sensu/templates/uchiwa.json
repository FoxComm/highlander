{
 "sensu": [
   {
       "name": "{{ uchiwa_dc_name }}",
       "host": "{{ sensu_api_host }}",
       "port": {{ sensu_api_port }},
       "timeout": {{ sensu_api_timeout }}
   }
 ],
 "uchiwa": {
   "port": {{ uchiwa_port }},
   "refresh": {{ uchiwa_refresh }}
  }
}
