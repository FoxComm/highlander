{
 "sensu": [
   {
       "name": "{{ uchiwa_dc_name }}",
       "host": "{{ sensu_api_host }}",
       "port": {{ sensu_api_port }},
       "timeout": {{ sensu_api_timeout }},
       "user": "{{ sensu_api_user }}",
       "pass": "{{ sensu_api_password }}"
   }
 ],
 "uchiwa": {
   "port": {{ uchiwa_port }},
   "refresh": {{ uchiwa_refresh }}
  }
}
