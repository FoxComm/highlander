---

- name: Download Lein Installer
  get_url:
    url: "{{ lein_installer_url }}"
    dest: /usr/local/bin
    mode: u+rwx
  sudo: yes

#- name: Trigger Installation Process
  #command: lein -v
  #sudo: yes
