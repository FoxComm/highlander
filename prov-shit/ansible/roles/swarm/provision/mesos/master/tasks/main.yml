---

- name: Copy Mesos Master Run Script
  template: src=run_mesos_master.sh dest=/usr/local/bin/ mode=750
  when:

- name: Restart Mesos Master
  service: name=mesos_master state=restarted enabled=true
