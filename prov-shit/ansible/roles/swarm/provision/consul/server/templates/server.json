{
    "ui-dir": "{{consul_web_ui_dir}}",
    "start_join_wan": [
        "{{consul_leader}}"
    ],
    "datacenter": "{{datacenter}}",
    "bind_addr": "{{inventory_hostname}}",
    "bootstrap_expect": {{master_ips|length}},
    "data_dir": "{{consul_data_dir}}",
    "encrypt": "{{consul_encryption_key}}",
    "client_addr": "0.0.0.0"
}
