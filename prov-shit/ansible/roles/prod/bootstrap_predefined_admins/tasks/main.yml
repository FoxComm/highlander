---

- name: Seed predefined admins
  shell: java -Ddb.host={{consul_hosts.db}} -Ddb.url="jdbc:postgresql://{{consul_hosts.db}}/{{db_name}}?user={{db_user}}" -Ddb.name={{db_name}} -cp {{phoenix_jar}} utils.seeds.Seeds seed --skipMigrate --skipBase --seedAdmins
  args:
    chdir: "{{phoenix_dir}}"
