---

- name: Provision Packer Tinystack Frontend
  hosts: all
  vars:
    user: ubuntu
    packing: true

  pre_tasks:
    - name: Wait for apt unlock
      shell: while sudo fuser /var/lib/dpkg/lock >/dev/null 2>&1; do sleep 1; done;
      become: true

  roles:
    - { role: base/mesos_worker }
    - { role: base/rsyslog, sudo: yes }
    - { role: base/balancer, sudo: yes }
    - { role: base/sensu, become: true, sensu_subscriptions: ["frontend"] }
