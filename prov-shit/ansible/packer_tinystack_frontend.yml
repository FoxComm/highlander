---

- name: Provision Packer Tinystack Frontend
  hosts: all
  vars:
    user: ubuntu
    packing: true

  roles:
    - { role: base/mesos_worker }
    - { role: base/rsyslog, become: true }
    - { role: base/balancer, become: true }
