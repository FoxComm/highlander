---

- name: Provision All The Things
  hosts: all
  vars:
    ashes_server_name: admin.foxcommerce.local
    storefront_server_name: foxcommerce.local
  roles:
    - { role: base/common }
    - { role: base/docker }
    - { role: base/mesos }

  handlers:
     - include: roles/base/consul_agent/handlers/main.yml
