---

- name: Provision JVM Build Machines
  hosts: all
  become: true
  vars:
    user: vagrant
  tags:
    - buildkite-agent

  roles:
    - { role: base/common }
    - { role: base/ssh_keys }
    - { role: base/scala }
    - { role: base/node }
    - { role: test/db }
    - { role: test/selenium }
    - { role: base/builder }
