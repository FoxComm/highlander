---

- name: Provision JVM Build Machines
  hosts: all
  vars:
    user: vagrant
  tags:
    - buildkite-agent

  roles:
    - { role: base/common }
    - { role: base/ssh_keys }
    - { role: base/scala }
    - { role: base/node, sudo: yes }
    - { role: test/db }
    - { role: test/selenium }
    - { role: base/builder }
