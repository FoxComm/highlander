---

- name: Packing Nanostack Worker Image
  hosts: all
  vars:
    user: ubuntu
    packing: true
  post_tasks:
    - name: Sweet Dreams
      pause: seconds=5

  roles:
    - { role: base/mesos_worker }
