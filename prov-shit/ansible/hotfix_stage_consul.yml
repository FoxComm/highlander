---

- name: Target Hotfix Stage Consul
  hosts:
    - stage-amigo-server
  vars:
    user: '{{ansible_ssh_user}}'
    join_type: "join-wan"

  roles:
    - { role: hotfix/consul_server }

  handlers:
     - include: roles/base/consul_agent/handlers/main.yml
