---

- name: Provision All The Things
  hosts: all
  vars:
    user: ubuntu
    packing: true
  post_tasks:
    - name: Sweet Dreams
      pause: seconds=5
    
  roles:
    - { role: base/docker }
    - { role: base/mesos_base }
