---

- name: Packing Base Swarm Image
  hosts: all
  vars:
    user: ubuntu

  sudo: true
  roles:
    # storage roles
    - { role: swarm/pack/vendor/consul/agent }
    - { role: swarm/pack/vendor/db }
    - { role: swarm/pack/vendor/flyway }
