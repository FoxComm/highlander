---

- name: Packing Swarm Worker Image
  hosts: all
  vars:
    user: ubuntu

  sudo: true
  roles:
    # worker roles
    - { role: swarm/pack/consul/agent }
    - { role: swarm/pack/mesos/worker }
