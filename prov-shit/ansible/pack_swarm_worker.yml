---

- name: Packing Swarm Worker Image
  hosts: all
  vars:
    user: ubuntu

  sudo: true
  roles:
    # worker roles
    - { role: swarm/pack/vendor/consul/agent }
    - { role: swarm/pack/vendor/mesos/worker }
