apis {
  middlewarehouse.url = ""

  stripe.key = ""
}

http.port = 9090

oauth {
  google {
    clientId = ""
    clientSecret = ""
    redirectUri = ""
    hostedDomain = ""
  }
}

users.admin.oauth = ${oauth}
users.customer.oauth = ${oauth}
