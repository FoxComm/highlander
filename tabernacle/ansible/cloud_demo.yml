---

- name: Provision Cloud Demo
  hosts: cloud-demo
  become: true
  vars:
    is_amigo: true
    storefront_server_name: example.foxcommerce.com
    user: "{{ ansible_user | default(lookup('env', 'USER')) }}"
  roles:
    - { role: dev/appliance }
    - { role: cloud/zookeeper }
    - { role: cloud/bigbag }
    - { role: dev/balancer }
    - { role: cloud/balancer }
