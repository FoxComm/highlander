---

- name: Update /etc/hosts for Kafka
  lineinfile: dest=/etc/hosts line="{{ansible_default_ipv4.address}} {{ansible_hostname}}"

- name: Restart Kafka and Schema Registry
  service: name={{item}} state=restarted
  with_items:
    - kafka
    - schema-registry
