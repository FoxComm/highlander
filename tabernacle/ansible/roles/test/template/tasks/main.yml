---

- name: Copy Template
  template: src=template.json dest=/tmp/ mode="u+rw,g+rw,o+r"

- name: Print Template
  shell: cat /tmp/template.json

- name: Remove Template
  file: path=/tmp/template.json state=absent
