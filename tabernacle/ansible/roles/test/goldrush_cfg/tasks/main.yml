---

- name: Debug Message
  debug:
    msg:
        - "Instance IP: {{private_ip}}"
        - "DNS Record: {{dns_record}}"
        - "Appliance hostname: {{appliance_hostname}}"
        - "Docker Tags: {{docker_tags}}"
        - "Marathon Flags: {{marathon_restart}}"
