---

- name: Enable Appliance Services
  service: name={{item}} enabled=yes
  with_items:
    - consul_agent
    - consul_template
    - dashboard
    - zookeeper
    - kafka
    - schema-registry
    - mesos_master
    - mesos_worker
    - marathon
    - elasticsearch
    - elasticsearch_5
    - kibana
    - postgresql
    - bottledwater_phoenix
    - bottledwater_middlewarehouse
    - materialized_views
    - pgweb

- name: Enable Marketplace Services
  service: name=bottledwater_marketplace enabled=yes
  when: with_marketplace

- name: Reload Systemd Daemon
  command: systemctl daemon-reload
