
import _ from 'lodash';
import { createSelector } from 'reselect';

export const selectableList = createSelector(
  state => state.countries.list,
  countries => _.map(countries, info => {
    return {
      value: info.name,
      id: info.alpha3,
    };
  }),
);

export default selectableList;
