
import { createReducer } from 'redux-act';

const initialState = {
  list: [
    {id: 234, name: 'UNITED STATES', alpha3: 'USA'},
    {id: 125, name: 'CANADA', alpha3: 'CAN'},
  ],
  details: {
    234: require('./mock/usa-details.json'),
  },
};

const reducer = createReducer({}, initialState);

export default reducer;
