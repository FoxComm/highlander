import fetch from 'isomorphic-fetch';

export function get(url) {
  return fetch(url).then(response => {
    if (response.status >= 400) {
      throw new Error('Bad status from server', response);
    }

    return response.json();
  });
}
