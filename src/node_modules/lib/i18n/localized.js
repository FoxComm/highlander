import localizedClassComponent from './localizedClassComponent';
import localizedFunctionComponent from './localizedFunctionComponent';

export default function localized(WrappedComponent) {
  return WrappedComponent.prototype.render
    ? localizedClassComponent(WrappedComponent)
    : localizedFunctionComponent(WrappedComponent);
}
