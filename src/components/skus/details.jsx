/**
 * @flow
 */

import React, { Component, Element, PropTypes } from 'react';

export default class SkuDetails extends Component<void, void, void> {
  render(): Element {
    return (
      <div>
        Details, motherfucker.
      </div>
    );
  }
}
