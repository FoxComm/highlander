/** Libs */
import React, { Component, PropTypes } from 'react';

/** Components */
import { SearchableList } from '../../list-page';

export default class CustomerItems extends Component {
  render() {
    return (
      <div>Customer items tab</div>
    );
  }
}
