import React, { PropTypes } from 'react';
import Form from '../forms/form';
import FormField from '../forms/formfield';

const Login = props => {
  return (
    <Form>
      <FormField
        className="fc-auth"
        label="Login">

      </FormField>
    </Form>
  );
};
