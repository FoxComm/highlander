export PHOENIX_URL="http://localhost"
export PHOENIX_PORT="9090"
export SOLOMON_URL="http://localhost"
export SOLOMON_PORT="4002"

export DB_USER=${DB_USER:-$USER}
export DB_HOST=${DB_HOST:-localhost}
export DB_NAME=onboarding_dev

if [ -f ".env.local" ]; then
    source .env.local
fi
