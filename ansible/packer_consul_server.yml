---

- name: Provision Consul Server Image
  hosts: all
  tags:
    - packer
  vars:
    user: ubuntu

  roles:
    - {role: base/common}
    - {role: base/consul}
    - {role: base/consul_server, consul_expect: 3}
