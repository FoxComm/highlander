---

- name: Provision Consul Server Image
  hosts: all
  tags:
    - packer
  vars:
    user: ubuntu
    packing: true

  roles:
    - {role: base/common}
    - {role: base/consul}
    - {role: base/consul_server}
