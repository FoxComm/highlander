{
    "service" : {
        "name": "{{search_service_name}}",
        "tags": ["{{search_service_name}}"], 
        "port": 9200,
        "checks": [
            {
                "http": "http://localhost:9200",
                "interval": "30s",
                "status": "passing"
            }
        ]
    }
}
