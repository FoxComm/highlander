---

- name: Provision Base Jvm Image
  hosts: all
  tags:
    - packer
  vars:
    user: ubuntu
    packing: true

  roles:
    - {role: base/consul_agent}
    - {role: base/consul_alerts}
    - {role: base/scala}
