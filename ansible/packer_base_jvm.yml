---

- name: Provision Base Jvm Image
  hosts: all
  tags:
    - packer
  vars:
    user: ubuntu

  roles:
    - {role: base/common}
    - {role: base/secret_keys}
    - {role: base/consul}
    - {role: base/consul_agent}
    - {role: base/scala}
