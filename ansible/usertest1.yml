---

- name: Provision Demo Backend
  hosts: usertest1-backend
  tags:
      - backend
  vars:
    user: '{{ansible_ssh_user}}'

  roles:
    - { role: buildkite_accessible, sudo: yes}
    - common
    - scala
    - kafka
    - elasticsearch
    - db
    - phoenix
    - { role: rsyslog, sudo: yes }
    - { role: kibana, sudo: yes }
    - greenriver
    - loyalty
    - messaging
    - seeder

- name: Provision Demo Ashes
  hosts: usertest1-ashes
  tags:
      - ashes
  vars:
    user: '{{ansible_ssh_user}}'

  roles:
    - { role: buildkite_accessible, sudo: yes}
    - common
    - { role: rsyslog, sudo: yes }
    - node
    - ssh_keys
    - ashes
    - firebird
    - { role: balancer, sudo: yes }
    - { role: demo_ashes, sudo: yes }
