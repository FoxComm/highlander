"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isElementInViewport = isElementInViewport;
exports.isElementVisible = isElementVisible;

// determines is element in visible viewport
function isElementInViewport(el) {
  var rect = el.getBoundingClientRect();

  return rect.top >= 0 && rect.left >= 0 && rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) && rect.right <= (window.innerWidth || document.documentElement.clientWidth);
}

// determines is element seeable and don't overlapped by other elements
function isElementVisible(element) {
  for (var _len = arguments.length, ancestors = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    ancestors[_key - 1] = arguments[_key];
  }

  var elements = [element].concat(ancestors);

  var _element$getBoundingC = element.getBoundingClientRect();

  var left = _element$getBoundingC.left;
  var right = _element$getBoundingC.right;
  var top = _element$getBoundingC.top;
  var bottom = _element$getBoundingC.bottom;


  var corners = [document.elementFromPoint(left + 1, top + 1), document.elementFromPoint(right - 1, top + 1), document.elementFromPoint(right - 1, bottom - 1), document.elementFromPoint(left + 1, bottom - 1)];

  return corners.every(function (corner) {
    return elements.includes(corner);
  });
}